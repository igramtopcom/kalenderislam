---
export function getStaticPaths() {
  return Array.from({ length: 9 }, (_, i) => ({
    params: { tahun: String(2024 + i) },
  }));
}
const { tahun } = Astro.params;
---
<html><body><p>Kalender Jawa {tahun} — placeholder</p></body></html>
